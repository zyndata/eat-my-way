# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Vite
.vite/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Env files (never commit secrets)
.env
.env.*
!.env.example

# USDA FoodData Central archives, cached by scripts/build-nutrition.mjs.
# Pinned by URL and SHA-256 in that script; the generated subset is committed, the
# multi-megabyte source archives are not.
data/usda/

# Test coverage
coverage/

# PWA generated files
dev-dist/

# Claude Code local overrides (machine-specific; the shared settings.json is committed)
.claude/settings.local.json
